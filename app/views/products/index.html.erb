<p id="notice"><%= notice %></p>

<h1>All products</h1>

<table>
  <thead>
    <tr>
      <th>Image URL</th>
      <th>Product and Description</th>
      <th>Price</th>
      <th> Availability </th>
      <th> Buy </th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= image_tag "products/#{product.image_url}" %></td>
        <td><span class="product_title"><%= link_to product.title,product_path(product) %></span><%= product.description %></td>
        <td><%= print_price(product.price) %></td>
        <td><%=print_stock(product.stock)%></td>
        <td><%=button_to 'Add to cart', carts_path(product_id: product.id)%></td>

      </tr>

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create a new Product', new_product_path %>
